import 'package:flutter/material.dart';

class MatopsTheme {
  static const Color defaultSuccess = Colors.green;
  static const Color defaultError = Colors.red;
  static const Color defaultSpinner = Colors.blue;
  static const Color defaultOverlay = Color(0x80000000);
}
